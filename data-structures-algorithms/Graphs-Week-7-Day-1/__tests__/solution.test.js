describe("UnitTests", function() {
  it("contains_an_adjacencyList", function() {
    // Failure message:
    // This test has no failure messages
    var g = new Graph();
    expect(g.adjacencyList).toEqual({});
  });
});
