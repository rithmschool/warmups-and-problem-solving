Write a function called flip_case. This function accepts a string and a letter and reverses the case of all occurrences of the letter in the string.

Examples

```py
flip_case("Hardy har har", "h") # "hardy Har Har"
flip_case("Aaaaahhh!", "A") # "aAAAAhhh!"
```
